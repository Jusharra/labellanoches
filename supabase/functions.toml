# Supabase Edge Functions Configuration

[abandoned-order-recovery]
# Run every 30 minutes to check for abandoned orders and send recovery messages
schedule = "*/30 * * * *"

[item-suggester]
# Function to provide intelligent menu item suggestions

[receipt-sender]
# Function to generate and send digital receipts via SMS/WhatsApp

[scheduleChecker]
# Run every minute to check for campaigns that are due to be sent
schedule = "*/1 * * * *"

[campaign-operations]
# Handles CRUD operations for campaigns

[contacts-operations]
# Handles CRUD operations for contacts and contact lists